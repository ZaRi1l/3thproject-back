rootProject.name = '3thproject_web'


toolchainManagement {
    jvm {
        javaRepositories {
            repository("foojay") {
                credentials {
                    // Foojay API는 인증이 필요 없습니다.
                }
            }
        }
    }
}